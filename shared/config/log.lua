---@class shared
shared = shared or {}

---@class shared.config
shared.config = shared.config or {}

---@class shared.config.log
shared.config.log = {}

---@type boolean true if debugging should be enabled
shared.config.log.debug = true

---@type boolean true if info logging should be enabled
shared.config.log.info = true

---@type boolean true if warning logging should be enabled
shared.config.log.warning = true

---@type boolean true if error logging should be enabled
shared.config.log.error = true
